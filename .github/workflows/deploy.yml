name: Build y publicar rama deploy

# El sitio se sirve desde Hostinger, que clona la rama `deploy` (contenido
# ya construido, index.html en la raíz). Cada push a main la regenera.

# La conexión de Git vive en el sitio `landing.tamaba.edu.ar` de hPanel,
# no en el de `tamaba.edu.ar`. Esa distinción es la que importa: el deploy
# aterriza en la carpeta del subdominio y no puede alcanzar la raíz del
# institucional. Verificado en producción el 2026-08-31.
on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: deploy
  cancel-in-progress: true

jobs:
  build-y-publicar:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Build con verificaciones
        run: node build.mjs --check

      - name: Publicar dist/ en la rama deploy
        run: |
          cd dist
          git init -b deploy
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Deploy: ${{ github.sha }}"
          git push -f "https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git" deploy
